/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
        API_URL: 'https://ecommerce-api-three-eta.vercel.app',
        // API_URL:'http://localhost:5000'
    },
};

export default nextConfig;
